spring:
  datasource:
    url: jdbc:mysql://sh-cynosdbmysql-grp-nq5kc648.sql.tencentcdb.com:20811/run_tracker?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 4daGaMPX
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    time-zone: Asia/Shanghai

app:
  wechat:
    app-id: YOUR_APP_ID
    app-secret: YOUR_APP_SECRET
  security:
    session-ttl-minutes: 4320

logging:
  level:
    org.springframework: INFO
